[package]
name = "blindpay_escrow"
version = "0.1.0"
edition = "2024_07"
description = "BlindPay STRK20 escrow anonymizer — privacy_invoke helper for private invoice settlement"
license = "MIT"
readme = "README.md"

[dependencies]
starknet = "2.12.1"
openzeppelin = "2.0.0"

[dev-dependencies]
snforge_std = "0.49.0"

[[target.starknet-contract]]
sierra = true
casm = true

[scripts]
test = "snforge test"

[tool.snforge]
exit_first = true
